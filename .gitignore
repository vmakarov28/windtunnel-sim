__pycache__/
*.pyc
.pytest_cache/
.venv/

# run outputs — regenerate from scene + seed, don't version
out/
out3d/
frames/
failures/
checkpoints/
*.mp4
*.mov

# validation figures ARE committed (validation/*.png is not ignored)

# web toy: generated WGSL dumps (regenerated by dump_wgsl.mjs for validation)
web/_build/

# video production work stays LOCAL (user request) — script, shot list,
# animation generators and rendered animation assets
video/
